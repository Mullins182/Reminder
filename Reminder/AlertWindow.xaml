<Window x:Class="Reminder.AlertWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Reminder"
        mc:Ignorable="d"
        Title="AlertWindow" Height="500" Width="500" WindowStartupLocation="CenterScreen" WindowState="Normal" WindowStyle="None" AllowsTransparency="True"
        Background="Transparent">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="90*"/>
            <RowDefinition Height="10*"/>
        </Grid.RowDefinitions>
        <TextBox x:Name="MessageBox" Background="#20FF0000" FontFamily="Copperplate Gothic Light" FontSize="35px" Foreground="Goldenrod"
                 TextWrapping="Wrap" TextAlignment="Center" Padding="25px" BorderBrush="#AAFF0000" Grid.Column="0" Grid.Row="0"/>
        
        <Button x:Name="CloseMessage" Width="150px" Height="50px" Background="Black" Foreground="Gold" Content="Close Message" FontSize="20px"
                Grid.Column="0" Grid.Row="1" Click="CloseMessage_Click"/>
    </Grid>
</Window>
